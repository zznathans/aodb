apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: {{ .Values.aodbApi.service.port }}
      targetPort: 8000
